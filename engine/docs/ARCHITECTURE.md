# Architecture — moved

The canonical QuizForge architecture guide now lives at **`dev/ARCHITECTURE.md`**
(repo root `dev/`). This older copy was stale (pre-`engine/` refactor) and has been
retired to avoid drift.

- Project orientation (one contract → three backends): root **`CLAUDE.md`**
- Engine design & data flow: **`dev/ARCHITECTURE.md`**
- File-by-file navigation: **`dev/AGENT_MAP.md`**
- API backend: **`api/README.md`**
