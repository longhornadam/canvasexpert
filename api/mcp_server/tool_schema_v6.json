{
  "schema_version": 6,
  "tools": [
    {
      "name": "get_authoring_contract",
      "required": ["kind"],
      "properties": {"kind": "string"}
    },
    {
      "name": "get_course_assignments",
      "required": ["course_id"],
      "properties": {"course_id": "string", "full_descriptions": "boolean"}
    },
    {
      "name": "get_gradebook_snapshot",
      "required": ["course_id"],
      "properties": {"course_id": "string"}
    },
    {
      "name": "get_modules",
      "required": ["course_id"],
      "properties": {"course_id": "string", "include_items": "boolean"}
    },
    {
      "name": "get_roster",
      "required": ["course_id"],
      "properties": {"course_id": "string"}
    },
    {
      "name": "get_seating_context",
      "required": ["course_id", "section_name"],
      "properties": {"course_id": "string", "section_name": "string"}
    },
    {
      "name": "get_submissions",
      "required": ["course_id", "assignment_id"],
      "properties": {
        "assignment_id": "string",
        "course_id": "string",
        "include_text": "boolean",
        "max_text_chars": "integer",
        "pseudonyms": "string"
      }
    },
    {
      "name": "list_courses",
      "required": [],
      "properties": {}
    },
    {
      "name": "refresh_mirror",
      "required": ["course_id"],
      "properties": {"course_id": "string"}
    }
  ]
}
