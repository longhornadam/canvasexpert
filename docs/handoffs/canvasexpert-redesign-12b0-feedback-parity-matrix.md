# Ferrari handoff 12b0: FeedbackExpert / PowerGrader parity matrix

Documentation only. Create `docs/reference/feedback-powergrader-parity.md` from actual
routes, templates, browser modules, pipeline helpers, storage, and tests. Rows must cover:
generic paste/import, contract validation, guided/OpenRouter batch scoring, Copilot/manual
results, CSV/New Quizzes, vault re-identification/review, persona/pattern selection,
folder actions, Canvas push, and Name Manager/privacy administration. For each record
current owner, data classification, inputs/outputs, external call, target PowerGrader
lane, compatibility requirement, test oracle, and gap. Do not change runtime code.

Run relevant Feedback/PowerGrader tests and `rg` call-site searches. One docs commit;
stop if an owner or privacy boundary is unclear. Ferrari must accept the matrix before
12b1.
