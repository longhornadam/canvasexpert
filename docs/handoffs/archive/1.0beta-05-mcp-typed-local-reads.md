# Route MCP roster and submission helpers through typed local scopes

Status: **GREEN; completed** — commit `047e882`

Risk: **high** - MCP returns pseudonymized private student information outside the app process;
wire allowlists, safety scanning, course gates, and vault behavior must remain exact.

Depends on: commit `ae69f26` (accepted 04 routine typed-read SDK)

## Teacher-visible result

MCP tools retain the same compact payloads and privacy gates while their local roster and
assignment-submission acquisition uses the typed read service instead of direct mirror
store/query ownership. Stale state still falls back live.

## Acceptance criteria

- [x] `_mirror_roster_doc` uses `read_service.private_roster` at the serve-age bound and preserves
      the existing monkeypatch/cache guard.
- [x] `_mirror_submission_bundle` reads typed roster, assignments, and submissions once each,
      requires all three current, filters the requested assignment locally, and uses the minimum
      required `last_success_at` as the unchanged `synced_at`.
- [x] Current local roster/submission tools make zero Canvas calls; any missing/stale/corrupt
      required scope falls back through the existing live/vault path as one coherent bundle.
- [x] Tool names, JSON schemas, columns, truncation, course gate, pseudonym mapping, vault
      transaction/conflict behavior, outbound scan, and structured errors remain unchanged.
- [x] `get_gradebook_snapshot` continues to use shared `gradebook_snapshot.load_snapshot`.
- [x] `mirror_store`/`mirror_queries` imports retained with caller proof (section map; serve-age).
- [x] The named acceptance gate passes.

## Locked decisions

- Preserve every module-level monkeypatch seam (`_course_*`, `_assignment*`, `_fetch_sections`)
  and `_cache_safe`. Typed envelopes are internal only; MCP output stays `source` + `synced_at`,
  no `generation`. Pseudonymize/gate before tabulation; trim text before the gate.

## Named acceptance gate

```powershell
py -m pytest api/tests/test_mcp_server_tools.py api/tests/test_beta075_mcp.py -q
```

## Execution result

- **Traffic light: GREEN.** Named gate: **42 passed** (37 pre-existing + 5 new).
- Changed files: `api/mcp_server/tools.py`, `api/tests/test_mcp_server_tools.py`
  (`test_beta075_mcp.py` and `docs/mcp-server.md` needed no change).
- Privacy verified by senior: both mirror and canvas branches converge on the identical
  `upsert_roster → pseudonymize_* → gate → _tabulate` pipeline; the typed reader only changes raw
  row acquisition. Pseudonymization, outbound `scan_payload` gate, course gate, and vault
  transaction/conflict behavior are unchanged and still covered (`test_get_roster_happy`,
  `test_get_submissions_happy_scrubs_real_name_and_short_name`,
  `test_no_pii_sweep_and_scan_payload_green`, `test_gate_fails_closed_on_leaking_identity_key`,
  `test_get_*_rejects_non_current_course`, `test_monkeypatched_fetch_seams_bypass_the_cache`).
- `mirror_store`/`mirror_queries` imports retained: `mirror_store.read_roster` for the section
  id→name map (no typed scope owns sections); `mirror_queries._serve_max_age_hours()` for the
  bound. The four former query helpers (`roster_freshness`, `data_freshness`, `assignment`,
  `assignment_submissions`) are no longer called from `tools.py`.
- Deviation: none. Implementation commit: `047e882`. No unresolved decisions.
