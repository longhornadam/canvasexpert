# Migrate Student Reports' per-student packet to local metadata + focused evidence

Status: **READY**

Risk: **medium** - read-only migration of a teacher-initiated, non-Canvas-mutating report
generator; the existing live fetch is preserved as the fallback path, not deleted.

Depends on: source HEAD `e40a2c0` (Batch 2 fully closed; Batch 5 is next per
`docs/handoffs/NEXT_BATCH.md`)

## Teacher-visible result

Generating a Student Report packet (`/students/reports`, "Standing/Late/Adjustments/Comments"
sections) for a student whose courses' private mirrors are current makes **zero Canvas calls**
for any of that metadata — it reads exactly what Gradebook/Roster/Course Info already trust.
Work-sample attachments still require a live fetch, but only for the one submission that
actually has a file to download, not the whole course's submissions. When a course's mirror
isn't current, the report falls back to exactly today's live fetch for that course, unchanged
— nothing that works today stops working.

One visible, deliberate content change: comments authored by staff now show a role label
("Instructor") instead of the commenter's name, matching a privacy rule the private mirror
already applies everywhere else (comments have never stored author names — see Background).
A student's own comments still show the student's own name, since the report already knows it.

## Background (read before writing code)

Vision doc §17.2 "Former Program 7 — migrate Student Reports and portfolios" (~lines
1375-1390): *"eliminate duplicate large metadata fetches from report generation... assemble
roster, assignment, standing, text, attempt, comment, group, and due facts from the typed read
service... remove routine direct `requests.Session` calls from report/portfolio code."* §11.7
(~lines 870-881): *"local spine: roster identity, assignments, submission standing, text
bodies, attempts, comments, group names, personalized due facts... focused Canvas/evidence:
attachment bytes, signed download URLs in memory... split current direct `requests.Session`
collection code into local metadata assembly plus focused evidence acquisition."* §7.3
(~lines 454-459): *"CanvasMirror coordinates [attachment/evidence] status... it does not turn
the heartbeat into a course-wide download job."*

Today, `api/student_packet.py::build_packet` opens its own `requests.Session()` (line 143) and
paginates the **whole course's** submissions for one student on every run — even though the
private mirror (`api/mirror/store.py`, Batch 1's typed read service in
`api/mirror/read_service.py`) already stores everything this report needs **except** file
attachments and the `url` field of an `online_url` submission, which are deliberately never
cached (signed URLs are never persisted, per the mirror's own design law).

**Senior decisions already made for this slice (do not revisit):**
- Reports keep their own narrow, focused per-submission fetch for attachments — this brief does
  **not** route through PowerGrader's `api/powergrader/canvas_fetch.py` evidence machinery.
  Confirmed: no code changes to anything under `api/powergrader/`.
- Staff-authored comments show a generic role label, not a name. A student's own comments show
  the report's own known `student_name`. This is the resolution to comments never having stored
  author names in the mirror to begin with (`api/mirror/store.py:557-569`'s `_comment_record`,
  its own comment: *"Role label only — still no names, avatars, or attachments"*) — not a new
  privacy reduction this brief introduces.
- This brief covers only `api/student_packet.py::build_packet` (the single-student,
  multi-course packet, used by both the interactive `/api/student-packet/stream` route and the
  `_run_routine_student_reports` built-in routine, which calls `build_packet` directly). The
  parallel migration of `api/portfolio_service.py::build_merged_portfolios` (single-course,
  multi-student cohort portfolios) is deliberately deferred to a follow-up brief that reuses
  this brief's new shared helpers — do not migrate it here.
- `api/webui/routes/reports.py::list_assignments_full` (the assignment-picker endpoint) is
  **not** touched by this brief — its rich shape (quiz detection, assignment-group names) has
  no equivalent yet in either `private_assignments` or Course Catalog's assignment scope, and
  closing that gap needs its own dedicated investigation, not a drive-by schema change here.
- The private source/freshness provenance manifest named by the vision doc is a separate,
  later, additive follow-up brief — not part of this one.

## Acceptance criteria

- [ ] `api/mirror/store.py::normalize_assignment` gains a new `description` field (the
  assignment's HTML instructions/prompt text, same permissive-string handling as the existing
  `body` field) — additive only; every existing field and caller is unaffected.
- [ ] `api/mirror/store.py::normalize_submission`'s `current` dict gains a new `url` field (the
  student's submitted URL for an `online_url` submission type, plain text — not a signed Canvas
  download URL, same sensitivity class as the already-stored `body`) — additive only.
- [ ] A new `api/submission_transport.py::fetch_submission(session, base, course_id,
  assignment_id, user_id)` function performs one focused, live Canvas fetch of exactly one
  student's one submission (for its current `attachments` list), returning `None` on any
  failure rather than raising — never a whole-course call.
- [ ] A new shared module assembles one course's joined assignment+submission records from
  `api/mirror/read_service.py` (`private_assignments`, `private_submissions`) in the same nested
  shape `build_packet`'s existing code already expects (`s["assignment"]`, `s["score"]`, etc.),
  when — and only when — both envelopes report `state == "current"`. When either is not
  current, the course is reported as needing the existing live fallback instead — the local
  path is never used with incomplete/stale data.
- [ ] `build_packet` uses the local path per course when available; when not, it falls back to
  exactly today's existing live `_get_all_pages` whole-course fetch for that one course,
  unchanged. A course that is local-current makes no whole-course Canvas call at all.
- [ ] For a local-current course, a live focused call via `fetch_submission` happens only for
  submissions whose `submission_type == "online_upload"` (the only type that can carry file
  attachments) and only when the "work" section is requested — never for `online_text_entry`
  or when "work" isn't in `sections`. A `requests.Session()` is constructed lazily, only the
  first time such a call is actually needed for a given `build_packet` run.
- [ ] A report that requests no attachments (either no "work" section, or a course whose
  submissions are all `online_text_entry`/`online_quiz`/ungraded with no upload) and whose
  every course is local-current makes **zero** Canvas calls — proven by a test asserting no
  Canvas-calling function is invoked.
- [ ] Comment display: for each `submission_comments` entry, if `author_id` matches the
  report's own `user_id`, the label is the report's own `student_name`; else if the comment's
  role (reusing the exact staff-role vocabulary already defined in
  `api/work_registry/providers/home_attention.py`: `_author_role`/`_PROVEN_STAFF_ROLES` —
  `{"admin", "administrator", "instructor", "staff", "ta", "teacher", "teaching_assistant"}`)
  is a proven staff role, the label is a generic role string (e.g. "Instructor"); otherwise the
  name is omitted entirely (no blank "— :" prefix) rather than guessed at.
- [ ] Every existing `_info_blocks`/`_signature`/"work"-section/manifest-dedupe behavior is
  otherwise unchanged — same DOCX output shape, same file-naming, same `_manifest.json`
  change-detection semantics, for both the local and live-fallback paths.
- [ ] The named acceptance gate passes.

The executor reports evidence; the senior accepts only when every criterion holds.

## Explicit non-goals

- Do not migrate `api/portfolio_service.py::build_merged_portfolios` — separate follow-up
  brief, reusing this brief's new shared module and `fetch_submission`.
- Do not migrate `api/webui/routes/reports.py::list_assignments_full` or touch Course Catalog's
  assignment schema.
- Do not build the private source/freshness provenance manifest the vision doc names — separate
  additive follow-up.
- Do not touch `api/powergrader/` (evidence/attachment code, PowerGrader's own report/packet
  concept) in any way.
- Do not change `_render_info_docx`, DOCX layout/styling, output file paths, `reports_root`
  resolution, or the existing `_manifest.json` skip-unchanged dedupe mechanism.
- Do not add caching/persistence for attachment bytes or URLs anywhere in the mirror — they
  stay live-fetched every time, exactly as today, per the vision's explicit rule against
  caching signed URLs.
- Do not add a TTL/`max_age_hours` gate on the new local reads beyond the mirror's own
  `state`/`error_code` signal — reports care about correctness/completeness, not an arbitrary
  age cutoff (pass `max_age_hours=None`).
- Do not change `api/webui/routes/routines_builtin.py::_run_routine_student_reports` — it
  calls `build_packet` directly and inherits this brief's behavior for free; do not add
  routine-specific logic.

## Locked decisions

- **Schema additions** (`api/mirror/store.py`):
  - `normalize_assignment` (~lines 522-536): add `"description": row.get("description") if
    isinstance(row.get("description"), str) else ""` to the returned dict, matching `body`'s
    existing permissive-string pattern.
  - `normalize_submission` (~lines 572-616): add `"url": row.get("url") if
    isinstance(row.get("url"), str) else ""` to the `current` dict (~line 583-608), alongside
    the existing `body` field. Do not touch `_attempt_record`/`_comment_record`/the `attempts`
    dict — this brief does not consume attempt history (no current caller needs it; do not add
    it speculatively).
- **New focused fetch** (`api/submission_transport.py`): add `fetch_submission(session, base,
  course_id, assignment_id, user_id) -> dict | None`, modeled on this file's existing
  `get_all_pages`/`download_binary` style (thin wrapper over a caller-supplied session, no
  session construction inside it). Hits Canvas's single-submission endpoint
  (`{base}/api/v1/courses/{course_id}/assignments/{assignment_id}/submissions/{user_id}`) to
  get that submission's current `attachments` list. Returns `None` on any non-200/transport
  failure — never raises, matching this module's existing style (its two current functions do
  raise via `response.raise_for_status()`; this new function must catch that and any transport
  exception itself and return `None`, since a missing/failed focused fetch must degrade to "no
  attachments found" for that one work sample, not abort the whole report).
- **New shared module** `api/report_local_reads.py` (or fold into `api/student_packet.py` if,
  after reading both files, a separate module proves to add more indirection than value for
  this single-consumer slice — senior prefers a separate module since the deferred
  `portfolio_service.py` follow-up will need the identical joining logic, but the executor may
  return YELLOW with a one-line justification if a shared module is clearly premature for this
  slice alone):
  - A function assembling, for one `course_id`, the joined per-submission records this report
    needs: read `read_service.private_assignments(course_id, max_age_hours=None)` and
    `read_service.private_submissions(course_id, max_age_hours=None)`; if either envelope's
    `state != "current"`, return a sentinel meaning "use the live fallback for this course"
    (e.g. `None`); otherwise, build a dict of submission records filtered to the requested
    `user_id`(s), each augmented with a nested `"assignment"` dict built from the matching
    assignment record's `id`/`name`/`due_at`/`points_possible`/`description` fields — matching
    exactly the shape `_info_blocks`/the "work" section loop already expect from `s.get(...)`
    and `s.get("assignment", {}).get(...)`.
  - A function resolving one comment's display label given `author_id`, `author_role`,
    `report_user_id`, `report_student_name` — implementing the locked comment-display rule
    above. Reuse `home_attention._author_role`/`_PROVEN_STAFF_ROLES` directly (import) rather
    than duplicating the role vocabulary, unless that import direction proves awkward (e.g. a
    circular import), in which case duplicate the frozen set with a comment noting it must stay
    identical to `home_attention.py`'s.
- **`build_packet` integration** (`api/student_packet.py:139-253`): inside the existing
  `for c in courses:` loop (~line 157), attempt the local assembly first; on the "use live
  fallback" sentinel, run exactly today's existing `_get_all_pages` call and downstream logic
  unchanged for that course. On a successful local assembly, skip the whole-course live fetch
  entirely for that course, and only call the new `fetch_submission` per `online_upload`
  submission when the "work" section is being written and `_download_binary` is about to run
  for attachments — construct the `requests.Session()` lazily (not unconditionally at line 143)
  so a fully-local run across all requested courses never touches `requests` at all. Every
  downstream call (`_info_blocks`, `_signature`, the "work" section's file-writing loop,
  `_manifest.json` read/write) operates on the assembled `subs` list exactly as it does today,
  regardless of which path produced it.

## Scope

- `api/mirror/store.py` (the two additive field changes only).
- `api/submission_transport.py` (new `fetch_submission` only).
- `api/report_local_reads.py` (new module) or the equivalent addition inside
  `api/student_packet.py` if the executor returns YELLOW justifying that instead.
- `api/student_packet.py` (`build_packet`'s course loop; no other function touched).
- `api/tests/test_mirror_store.py` (new-field coverage), a new `api/tests/test_submission_transport.py`
  or extension if one already exists (`fetch_submission` coverage), a new
  `api/tests/test_student_packet.py` (this function has zero existing tests today — this brief
  establishes its first coverage), and whichever existing `read_service` test file covers
  `private_assignments`/`private_submissions` (confirm exact name in preflight).

## Read only these references

- `AGENTS.md`
- Vision doc sections only, in `docs/reference/canvasmirror-1.0beta-information-spine.md`:
  §7.3 "Focused evidence remains a separate owner" (~lines 454-459), §11.7 "Student Reports and
  portfolios" (~lines 870-881), §17.1 row 5, §17.2 "Former Program 7" (~lines 1375-1390) —
  nothing else in that document.
- `api/mirror/store.py::normalize_assignment`, `::normalize_submission`, `::_comment_record`,
  `::_attempt_record` (read-only for the last one — do not add attempt exposure)
- `api/mirror/read_service.py` in full (216 lines — it is short and every function is relevant
  context: `private_assignments`, `private_submissions`, `_private_document`, `_envelope`)
- `api/submission_transport.py` in full (30 lines)
- `api/student_packet.py` in full (254 lines — this is the entire file being migrated)
- `api/work_registry/providers/home_attention.py::_author_role`, `::_PROVEN_STAFF_ROLES`,
  `::_is_proven_staff` (read-only, to reuse or faithfully copy the role vocabulary)
- `api/webui/routes/reports.py::api_student_packet_stream` (the interactive call site — read
  only to confirm its call signature to `build_packet` doesn't need to change)
- `api/webui/routes/routines_builtin.py::_run_routine_student_reports` (read only, to confirm
  it calls `build_packet` directly and needs no change)
- Existing mirror-first/live-fallback precedent (read only, as a pattern reference — do not
  copy verbatim, the shapes differ): `api/webui/routes/roster.py::_group_categories_for_roster`

Do not read: `docs/handoffs/archive/` (all of it — nothing there concerns Student Reports),
`api/portfolio_service.py`, `api/portfolio.py`, `api/nq_report.py` (out of scope this slice),
anything under `api/powergrader/`, or the rest of the vision doc.

## Preflight - stop if these facts are false

```powershell
rg -n "def normalize_assignment|def normalize_submission|def _comment_record|def _attempt_record" api/mirror/store.py
rg -n "def private_assignments|def private_submissions|def _private_document" api/mirror/read_service.py
rg -n "def get_all_pages|def download_binary" api/submission_transport.py
rg -n "def build_packet" api/student_packet.py
rg -n "def _author_role|_PROVEN_STAFF_ROLES" api/work_registry/providers/home_attention.py
rg -n "build_packet" api/webui/routes/reports.py api/webui/routes/routines_builtin.py
```

- `normalize_assignment`/`normalize_submission` still return exactly the field sets documented
  above with no `description`/`url` field already present (else this brief is redundant/stale).
- `private_assignments`/`private_submissions` still take `(course_id, *, root=None,
  max_age_hours=None, now=None)` and still return the same envelope shape (`state`, `records`,
  etc.) documented above.
- `build_packet` still opens its own `requests.Session()` unconditionally at the top of the
  function (not already lazy) and still fetches the whole course's submissions via
  `_get_all_pages` inside the `for c in courses:` loop with no local-mirror check already
  present.
- `_author_role`/`_PROVEN_STAFF_ROLES` still exist with the exact role-string set quoted above.
- Both `reports.py` and `routines_builtin.py` still call `build_packet` with the same positional
  signature (`user_id, student_name, sections, courses, base, token, reports_root,
  curve_events, skip_unchanged=...`) — no other call site exists.

If any fact is false, return RED without implementation changes.

## Known baseline (only when relevant)

Source HEAD `e40a2c0`: `py -m pytest api/tests -q` → 1052 passed, 1 skipped. This brief neither
fixes nor re-litigates that baseline.

## Named acceptance gate

```powershell
py -m pytest api/tests/test_mirror_store.py api/tests/test_student_packet.py api/tests/test_submission_transport.py -q
```
(adjust file names to whatever the executor actually creates/finds per Scope above; report the
exact command used)

- New focused tests must prove: (a) `normalize_assignment`/`normalize_submission` round-trip
  the two new fields and leave every other field unchanged; (b) `fetch_submission` returns
  `None` (not a raise) on a non-200/transport failure; (c) a `build_packet` course whose local
  mirror is fully current produces the exact same `_info_blocks` content the old live fetch
  would have, with zero calls to any Canvas-calling function, when no attachment work samples
  are involved; (d) a course whose mirror is not current (`state` other than `"current"`, or no
  document at all) falls back to the existing live fetch, unchanged; (e) an `online_upload`
  submission in a local-current course triggers exactly one `fetch_submission` call (not a
  whole-course call) when "work" is requested, and zero when it isn't; (f) comment-author
  display resolves to the student's own name, a role label, and omitted-name correctly across
  the three cases.
- No browser render is required: no template, shared script, navigation, or browser safety
  control changes.

## Stop conditions

- **RED:** `private_assignments`/`private_submissions` do not actually expose every field
  `_info_blocks`/the "work" section need beyond the two named additions (contradicting this
  brief's own field analysis); a Canvas endpoint this brief relies on needs a scope/permission
  this app doesn't already request; or preserving `build_packet`'s exact downstream shape turns
  out to require changes to `_info_blocks`/`_signature`/the "work" loop themselves (this brief
  locks those as unchanged).
- **YELLOW:** a separate `api/report_local_reads.py` module proves clearly premature for one
  consumer (see Locked decisions); the `home_attention` role-vocabulary import direction is
  awkward enough to warrant senior confirmation before duplicating the constant; or the exact
  Canvas single-submission endpoint/query shape needs a design choice not already covered here.

## Execution result

**GREEN** — every acceptance criterion holds and the named gate passes with zero new
failures. First execution pass flagged two locked-decision interpretations (comment-display
scope, drop-vs-blank) as open questions; the senior reviewed the diff directly, ruled both
were regressions rather than acceptable readings, and directed the two corrections recorded
under "Senior-directed corrections" below. Both are now implemented and covered by tests.

Commit: `e595c47`.

### Preflight
All six `rg` commands were run exactly as written; every stated fact held true:
- `normalize_assignment`/`normalize_submission` had no `description`/`url` field yet.
- `private_assignments`/`private_submissions` still take `(course_id, *, root=None,
  max_age_hours=None, now=None)` and return the same envelope shape.
- `build_packet` still opened `requests.Session()` unconditionally at the top of the
  function and fetched the whole course's submissions via `_get_all_pages` inside the
  `for c in courses:` loop, no local-mirror check present.
- `_author_role`/`_PROVEN_STAFF_ROLES` existed with the exact quoted role set.
- Both `reports.py::api_student_packet_stream` and
  `routines_builtin.py::_run_routine_student_reports` called `build_packet` with the same
  positional signature.
No stop condition triggered; proceeded to implementation.

### Acceptance criteria
- [x] `normalize_assignment` gains `"description"` (permissive-string, same pattern as
  `body`), additive only — `api/mirror/store.py:522-537`. Round-trip + non-string-default
  coverage: `test_mirror_store.py::test_normalize_assignment_description_is_permissive_string_like_body`,
  and the existing `test_assignments_round_trip_slim_shape` updated to assert the field is
  now stored (its old fixture comment, "must not be stored," was the pre-brief truth this
  slice deliberately changes).
- [x] `normalize_submission`'s `current` dict gains `"url"` (same permissive-string pattern),
  additive only — `api/mirror/store.py:583-608`. `_attempt_record`/`_comment_record`/the
  `attempts` dict were not touched. Covered by
  `test_current_carries_url_field_with_permissive_string_handling` and
  `test_current_url_defaults_to_empty_string_when_absent_or_non_string`.
- [x] `api/submission_transport.py::fetch_submission(session, base, course_id,
  assignment_id, user_id)` added: one focused GET to the single-submission endpoint,
  catches `raise_for_status()` and any transport exception, returns `None` rather than
  raising — never a whole-course call. Covered by `test_submission_transport.py` (success,
  HTTP 404, `requests.ConnectionError`, and a generic `ValueError` all return `None`/the
  parsed JSON as appropriate; the exact URL and timeout are asserted).
- [x] New shared module `api/report_local_reads.py` (separate module — the default per
  Locked decisions; no YELLOW justification needed).
  `local_course_submissions(course_id, user_id, *, root=None)` reads
  `read_service.private_assignments`/`private_submissions` with `max_age_hours=None`,
  returns `None` unless **both** envelopes report `state == "current"`, else joins
  submissions filtered to `user_id` with a nested `"assignment"` dict
  (`id`/`name`/`due_at`/`points_possible`/`description`) matching exactly what
  `_info_blocks`/the "work" loop already read via `s.get(...)` /
  `s.get("assignment", {}).get(...)`.
- [x] `build_packet`'s `for c in courses:` loop (`api/student_packet.py:171-195`) tries
  `report_local_reads.local_course_submissions` first; on `None` it runs exactly the
  original `_get_all_pages` call and the original `except requests.HTTPError` /
  `except Exception` handling, byte-for-byte unchanged (only `session` renamed to the
  lazy `_session()` call). A local-current course makes no whole-course Canvas call.
- [x] The focused `fetch_submission` call fires only inside `if "work" in sections:`, only
  for `used_local` courses, only for `s.get("submission_type") == "online_upload"` —
  never for `online_text_entry`/when "work" isn't requested. `requests.Session()` is
  constructed lazily via a `_session()` closure the first time either the live fallback
  or this focused fetch actually needs it (`api/student_packet.py:148-157`).
- [x] Zero-Canvas-calls proof: `test_build_packet_makes_zero_canvas_calls_when_local_current_and_no_attachments`
  monkeypatches `_get_all_pages`, `_fetch_submission`, `_download_binary`, **and**
  `requests.Session` itself to raise `AssertionError` if invoked, then asserts the run
  completes with no "skipped" line and a written Info document — proving zero calls, not
  just fewer.
- [x] Comment display: self (`author_id == user_id`) shows `student_name`; a proven-staff
  role (imported directly from `home_attention._PROVEN_STAFF_ROLES`/`_author_role`, no
  duplication needed — the import direction was not awkward, no circular import) shows
  the generic label `"Instructor"`; any other author's name is blanked (`""`) — never
  guessed at, never dropped, and only applied to the local-current path (see
  "Senior-directed corrections"). Covered by
  `test_apply_comment_display_resolves_self_staff_and_blank_cases` (unit),
  `test_info_blocks_omits_name_prefix_but_keeps_comment_when_author_name_is_blank`,
  `test_build_packet_end_to_end_keeps_unattributable_local_comment_without_name`, and
  `test_build_packet_live_fallback_never_relabels_comments`.
- [x] `_signature`, `_render_info_docx`, the "work" file-writing loop's logic, and
  `_manifest.json` semantics were not edited at all — only `build_packet` itself (plus its
  own nested `_session` closure) changed, plus one senior-authorized minimal edit to
  `_info_blocks`'s comment-line formatting (see "Senior-directed corrections").
  `test_local_assembly_produces_identical_info_blocks_to_the_old_live_shape` directly
  asserts `_info_blocks(local_subs, ...) == _info_blocks(live_shaped_subs, ...)` for
  standing/late/adjustments/comments given equivalent underlying facts and no comments.
- [x] Named acceptance gate passes (see below).

### Senior-directed corrections (resolved)
First execution pass flagged two locked-decision interpretations as open questions. The
senior read `api/report_local_reads.py` and the `build_packet` diff directly and ruled both
were unintended regressions, not acceptable readings. Both are now fixed as directed:

1. **Comment-display rule now applies only to the local-current path.**
   `report_local_reads.apply_comment_display` is called in `build_packet` only when
   `used_local` is `True` (`api/student_packet.py:195-201`). Live-fallback `subs` come
   straight from a live Canvas call and already carry real `author_name` values exactly as
   `_info_blocks` reads them today — the mirror's "no author names" rule is a property of
   the *mirror* (it never stored them), not a report-wide privacy reduction the brief
   intended to newly impose on data the report legitimately already has. Fixed per senior
   ruling; my original reading (apply uniformly to both paths) was wrong — it would have
   made a course whose mirror simply hadn't synced yet show *worse* comment attribution
   than it did yesterday, contradicting the brief's own "nothing that works today stops
   working" for the fallback case. Covered by
   `test_build_packet_live_fallback_never_relabels_comments` (monkeypatches
   `apply_comment_display` to raise if invoked on the live-fallback path; run completes
   clean).
2. **Unattributable comments are kept, not dropped — name blanked instead.**
   `comment_author_label` now returns `""` (not `None`) for the unattributable case;
   `apply_comment_display` keeps every comment, only blanking `author_name`. The one
   senior-authorized, minimal exception to "no other function touched":
   `_info_blocks`'s comment-line formatting (`api/student_packet.py:114-121`) now branches
   on whether `who` is truthy — `f'{when} — {who}: {comment}'` when a label is present
   (byte-identical to before), `f'{when}: {comment}'` when blank (no dangling `" — :"`).
   Senior's own re-reading of the acceptance criterion — "omitted entirely... rather than
   *guessed at*" — means "don't guess a name," not "delete the comment"; preserving
   comment content (the whole point of the Comments section) outweighs the "no other
   function touched" lock in this one narrow case. Covered by
   `test_info_blocks_omits_name_prefix_but_keeps_comment_when_author_name_is_blank` and
   `test_build_packet_end_to_end_keeps_unattributable_local_comment_without_name`.

### Deviations / design notes (not stop conditions)
- **`api/tests/test_canvas_read_service.py` was not extended.** The brief's Scope names
  "whichever existing read_service test file covers private_assignments/private_submissions
  (confirm exact name in preflight)" — confirmed as this file — but `read_service.py`
  itself was not modified (it passes through whatever `store.py` returns unchanged), so
  there was no new read_service-level behavior needing coverage there. The two new
  fields' round-trip proof lives in `test_mirror_store.py`, matching the named
  acceptance gate's own description of item (a).
- **`local_course_submissions` takes one `user_id`, not a collection**, since
  `build_packet` is the only current consumer and only ever needs one student per call.
  Per AGENTS.md's lean-engineering default ("add no adapter... without an immediate
  consumer"), I read the brief's "user_id(s)" as anticipating the deferred portfolio
  brief's future signature need, not a requirement to build multi-user plumbing now.

### Changed files
- `api/mirror/store.py` — `normalize_assignment` gains `description`;
  `normalize_submission`'s `current` dict gains `url`. Additive only.
- `api/submission_transport.py` — new `fetch_submission`.
- `api/report_local_reads.py` (new) — `local_course_submissions`, `comment_author_label`
  (blanks rather than drops), `apply_comment_display`.
- `api/student_packet.py` — `build_packet`: lazy `_session()`, local-first/live-fallback
  per course, focused attachment fetch for `online_upload` under "work",
  comment-display transform applied only when `used_local`; plus the one
  senior-authorized `_info_blocks` comment-line formatting branch (blank-name case).
- `api/tests/test_mirror_store.py` — updated `test_assignments_round_trip_slim_shape`
  (now expects `description`); 3 new tests for the two additive fields.
- `api/tests/test_submission_transport.py` (new) — 4 tests for `fetch_submission`.
- `api/tests/test_student_packet.py` (new) — 10 tests covering acceptance-gate scenarios
  (c)-(f), the two fallback variants under (d), the blank-name `_info_blocks` formatting,
  and the live-fallback-never-relabels correction.

### Named acceptance gate
```
py -m pytest api/tests/test_mirror_store.py api/tests/test_student_packet.py api/tests/test_submission_transport.py -q
46 passed
```

Full API suite (integration checkpoint, since `store.py` and `student_packet.py` are
broad-surfaced):
```
py -m pytest api/tests -q
1069 passed, 1 skipped
```
Known baseline was `1052 passed, 1 skipped` at `e40a2c0`; the delta is exactly the 17 new
tests added by this brief (3 + 4 + 10), no other change and no new failure.
