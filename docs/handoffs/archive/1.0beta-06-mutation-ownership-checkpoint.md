# Establish a machine-checked Canvas mutation ownership boundary

Status: **GREEN; completed** — commit `1d85660`

Risk: **medium** - no runtime behavior changes, but an incomplete ownership contract would hide
beta-blocking write/reconciliation gaps.

Depends on: commit `047e882` (accepted 05 MCP typed local reads)

## Teacher-visible result

No UI change. Every outbound mutation call is now machine-accounted with its owner, transport,
affected local scopes, and present reconciliation state — a bounded source for the Batch 7
implementation briefs without rereading the repository.

## Acceptance criteria

- [x] Strict, student-free `docs/contracts/canvas-transport-owners.json` lists every detected
      non-test mutation call by relative path + qualified symbol + call form, with classification,
      affected scopes, reconciliation state, and a gap reason.
- [x] `api/tests/test_canvas_mutation_ownership.py` AST-scans `api/` (excluding tests) and fails on
      unlisted mutation calls, and on stale/duplicate owners or unknown classifications/scopes/
      states, absolute paths, URLs, secrets, or student data.
- [x] Synthetic unlisted-call and stale-listed-owner failure cases prove the scanner fails.
- [x] `docs/reference/mutation-reconciliation-map.md` groups the contract into Batch 7 families and
      names covered paths vs gaps; calls no gap GREEN.
- [x] Diagnostics, external-AI, generic transport internals, uploads, and native New Quiz transport
      are explicitly classified, not excluded.
- [x] The named acceptance gate passes; counts reported below.

## Named acceptance gate

```powershell
py -m pytest api/tests/test_canvas_mutation_ownership.py api/tests/test_operation_ledger.py -q
```

## Execution result

- **Traffic light: GREEN.** Named gate: **29 passed** (7 new + 22 existing operation-ledger).
- Changed files (all new, no runtime edits): `docs/contracts/canvas-transport-owners.json`,
  `api/tests/test_canvas_mutation_ownership.py`, `docs/reference/mutation-reconciliation-map.md`.
- The AST scanner is intentionally broader than the brief's literal preflight greps: it also
  catches `canvas.post/put` in standalone scripts, bare-name `canvas_send`/`_canvas_send` imports,
  and mutation-verb alias assignments (e.g. `http_post = requests.post`). No dynamic-dispatch case
  defeated static classification — RED not triggered.
- **Totals (56 owners).** Classification: `canvas_mutation` 44, `canvas_read_acquisition` 5,
  `canvas_upload` 2, `generic_transport_internal` 2, `canvas_mutation_native` 1,
  `diagnostic_probe` 1, `external` 1. Reconciliation: `none` 31, `n/a` 17, `targeted` 7,
  `invalidate` 1. Zero `unknown` scopes.
- **Named Batch 7 gap seeds (all reconciliation `none`):** (1) catalog-structure writes (19
  owners) verify Canvas but never invalidate the catalog projection; (2) grades/curves/sweeps
  writes have no reconciliation unlike PowerGrader's reconciled grade-push; (3) per-student
  assignment-fact overrides (`private.assignments`) have no invalidate; (4) two duplicate-
  implementation splits (group membership/category, and late policy) where a direct route
  reconciles but a ledger adapter does not — Batch 7 must confirm live callers before reconciling
  vs retiring (Former Program 10).
- Interpretive note (senior-confirmed acceptable): per-student assignment overrides mapped to
  `private.assignments` as the closest vocabulary fit, flagged in the JSON/map rather than left
  `unknown`.
- Deviation: none from locked scope/vocabulary. Implementation commit: `1d85660`.

## Senior closure note

Batch 6 (Home, Work, Routines, MCP, derived views) is complete: units 01–06 accepted. The
DeepSeek V4 Flash packet stops here by design. Batch 7 reconciliation briefs must be authored
from this contract/map; the duplicate-implementation questions (gap 4) need a senior live-caller
decision before those briefs are written.
