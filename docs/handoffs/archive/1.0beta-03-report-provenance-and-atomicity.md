# Make report comment provenance honest and its sidecar atomic

Status: **GREEN; completed** — commit `5bc929c`

Risk: **medium** - private Student Reports/portfolio metadata and fallback selection only; no
Canvas write or rendered DOCX contract changes.

Depends on: commit `f482042` (accepted 02 Home comment-aware reads)

## Teacher-visible result

Reports use local comments only inside the same bounded freshness policy as Home. Their private
source manifest reports the oldest required scope timestamp and cannot be torn by interruption.

## Acceptance criteria

- [x] The shared report join requires current typed assignments plus current
      `PRIVATE_SUBMISSION_COMMENTS` at the configured serve-age bound; otherwise it preserves the
      existing live Canvas fallback.
- [x] `_source_manifest.json` reports `source=mirror` only for that local path and uses the
      minimum assignment/comment-inclusive timestamp; fallback remains `source=canvas` with no
      claimed sync timestamp.
- [x] Missing/corrupt/aged comment state cannot be labeled mirror-current.
- [x] `write_source_manifest` writes a same-directory temporary file, flushes and fsyncs it,
      then `os.replace`s the destination; failure leaves the prior destination readable and
      cleans the temporary file best-effort.
- [x] DOCX content, evidence fetches, `_manifest.json` dedupe behavior, report roots, and private
      manifest allowlist remain unchanged.
- [x] The named acceptance gate passes.

## Locked decisions

- Reuse the dedicated comment scope's existing normalized rows; do not join a third duplicate
  submission read. Apply `mirror_queries._serve_max_age_hours()` consistently to assignment and
  comment scopes.
- Keep manifest entry keys exactly `course_name, source, synced_at, generated_at`.
- Implement atomic replacement locally in `report_local_reads.py`; do not introduce a generic
  persistence abstraction. Temp files contain private data and must stay in `dest_dir`.
- Update test mirror fixtures by recording comment state current; do not weaken prior assertions.

## Named acceptance gate

```powershell
py -m pytest api/tests/test_report_local_reads.py api/tests/test_student_packet.py api/tests/test_portfolio_service.py -q
```

## Execution result

- **Traffic light: GREEN.** Named gate: **40 passed**.
- Changed files: `api/report_local_reads.py`, `api/tests/test_report_local_reads.py`,
  `api/tests/test_student_packet.py`, `api/tests/test_portfolio_service.py`.
  `student_packet.py` / `portfolio_service.py` untouched (no signature change needed).
- `write_source_manifest` uses `tempfile.mkstemp(dir=dest_dir)` + flush + `os.fsync` + `os.replace`,
  with best-effort temp cleanup and exception propagation on failure (`os.replace` overwrites
  atomically on Windows for closed handles — no YELLOW encountered).
- `local_course_freshness` gates on `private_assignments` AND `private_submission_comments` at
  `_serve_max_age_hours()`, `synced_at` = min of both `last_success_at`.
- Deviation: none from locked decisions. Test-only isolation: the mirror-current fixtures
  monkeypatch `_serve_max_age_hours` to a large value so fixed historical stamps stay current;
  dedicated aging tests override it to a small real bound to prove the canvas fallback. No prior
  assertion weakened.
- Implementation commit: `5bc929c`. No unresolved decisions.
