# SENIOR ONLY — CanvasMirror 1.0beta batch pointer

> **NOT EXECUTOR AUTHORITY.** DeepSeek V4 Flash must read `AGENTS.md`,
> `docs/handoffs/CURRENT.md`, and only the references routed by that brief. It must not use
> this file to widen or reinterpret its assignment.

## Current state

Batch 6 — Home, Work, Routines, MCP, and derived views — is active. Its immediate unit is
**05 — MCP typed local reads** (promoted from the external candidate packet). The brief in
`CURRENT.md` is the sole implementation authority. 01 (`6103fca`), 02 (`f482042`),
03 (`5bc929c`), and 04 (`ae69f26`) are GREEN, accepted, and archived.

## Promotion after 05 acceptance

Promote **06 — mutation ownership checkpoint**. Its candidate brief is outside the repository
execution surface in the senior's packet; it is not authority until reviewed and copied to
`CURRENT.md` with an accepted commit in its `Depends on` line. The packet deliberately stops
after 06; the remaining reconciliation families and final beta acceptance must be authored
from 06's exact map.

## Batch table status

- Batches 0–5: delivered or accepted to their recorded scope.
- Batch 6: active; 01 + 02 + 03 + 04 accepted, 05 (MCP typed local reads) is current.
- Batches 7–8: not active and must not start early.

---

Overwrite this file—never append—when the senior accepts `CURRENT.md` or changes the
authoritative batch.
